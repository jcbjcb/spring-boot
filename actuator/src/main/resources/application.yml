server:
  port: 8081

info:
  app:
    name: spring-boot-actuator-demo
    version: 1.0
    test: test

management:
  endpoints:
    web:
      exposure:
        include: "*"
      base-path: /actuator
  endpoint:
    health:
      show-details: always
    shutdown:
      enabled: true
